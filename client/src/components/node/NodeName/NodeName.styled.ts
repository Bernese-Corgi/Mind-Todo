import { EditDeleteButtonUnit } from 'components/common';
import styled from 'styled-components';
import theme from 'styles/theme';

export const StyledNodeNameEditUnit = styled(EditDeleteButtonUnit)`
  .editTextArea {
    font-weight: ${theme.fonts.weight.bold};
  }
`;
